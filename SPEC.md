# 白嶺岳 ― 登山口から山頂へ

## ゲーム概要

標高 3,026m の白嶺岳を、登山口から自分のペースで登る三人称／一人称の歩行シミュレータ。体力・水・時刻が減り、山小屋で回復する。

## 対象端末

- iPhone Safari（主戦場）とデスクトップブラウザ
- 公開先は GitHub Pages。エントリは `index.html`

## 画面 / 状態遷移

- タイトル → プレイ → 一時停止（操作設定） → 再開
- 山頂到達で登頂画面
- 開始直後の 4.2 秒は、登山者の後方上空 17m からカメラが寄ってくる。移動を始めると 0.7 秒で通常の追従に切り替わる
- 小屋の半径 26m 以内では、カメラが建物全体を収める引きの位置に移る
- タイトルは遠景だけなので、影・雲・草木を止めて描画を軽くする。登り始めで戻す

## 操作

- 移動: 左スティック（タッチ）／ WASD。右ドラッグで見回す
- 走る: 「走る」ボタンを押している間、または Shift。息が上がると走れない
- 倍速: 「倍速」ボタン、または T。押すたびに通常と 10 倍を切り替える。10 倍のあいだは移動だけを 10 回に分けて進める。体力消費は通常歩行と同じ比率
- 調べる: 対象に近づくとボタン（Q）
- 小屋: 近づくと「小屋で休む」（E）。45 分休憩して体力・水・食料が戻る
- 小休止 R、視点 V、挨拶 G、行動食 F、サル H、音・一時停止は右上
- 道の登りは 1m あたり 0.22（約12度）まで。それより急な岩は道の外に残し、道の両脇は歩く高さにならす
- 歩ける／歩けないの判定は、1.2m 先の高さで測る。目の前の段差は 45cm まで越えられる（道の縁の 22cm の段差で止まらないため）
- 登山口は標高 1,571m。樺平小屋まで 4.8km、山頂まで 10.1km
- 操作設定（タイトルの「操作設定」と一時停止）: 見回す感度 弱/中/強、上下反転、振動、音。保存キーは `tg.261.ctrl` と `tg.261.mute`

## コアループ

歩く → 急登で体力と水が減る → 小屋か小休止で回復 → 山頂

## 勝敗条件

- 山頂（原点から 14m 以内）に着くと登頂
- ゲームオーバーは無い。息切れと水切れは警告して歩ける

## UI

- 標高、時刻、気温、風、酸素、体力、水、食料、次の地点
- 倍速ボタンは右下。オンの表示は「10倍」

## 音声

- 開始タップで WebAudio を unlock。音が無くても進行は止めない
- 風、沢、足音、息。ミュートは `tg.261.mute`

## 保存

- ベストタイム `tg.261.best`（旧 `hakurei-best` は読むだけ）
- 操作設定 `tg.261.ctrl`

## ファイル構成

- `index.html` `style.css` `script.js` `three.min.js`
- 外部アセットは Web フォントのみ

## 実装制約

- 公開実体は 20MB 以下
- 入力は Pointer Events。ボタンは `pointerdown` で反応し、`setPointerCapture` を使う
- iOS: viewport-fit=cover、safe-area、touch-action、ダブルタップ防止

## テスト項目

- 倍速を押すと表示が「10倍」になり、もう一度で「倍速」に戻る
- 小屋に近づくと屋根・壁・入口が一度に画面に入る
- 一時停止に感度・上下反転・振動・音がある

## 未確定事項

- なし
