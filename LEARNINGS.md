# LEARNINGS

## 2026-09-25

- 道のリボンを地形の左右端の高さで張ると、20m グリッドの斜面で砂利が壁になる。横断勾配 0.62 以上、または進行勾配 0.72 以上の区間は砂利を出さず鎖場にし、歩行時だけ勾配 1.7 まで進める。
- 小屋のカメラをプレイヤー後方 4m のままにすると、赤い屋根が画面を埋める。小屋中心から経路側へ 22m、高さ +9m に引くと屋根・壁・入口が同時に入る。
- 道の最大登りは 1m あたり 0.22（約12度）。それ以上は折り返して距離を稼ぐ。道の両側 32m は、その道の高さにならす。いちばん急な地点で、進行方向の地面は 0.06、左右は 0.01 だった。
- 歩ける勾配を「1フレームで動いた距離」で割ると、道の縁にある 22cm の段差が勾配 2 以上の壁として判定される。急でない場所で「登れない」が出た原因はこれ。勾配は 1.2m 先で測り、目の前の段差は 45cm まで越えられるようにした。
- 生成を同期で回すと、読み込み中は画面もタップも止まる（harness で 2 RAF/秒・タップ timeout）。地形ループを行単位、草木を数百本単位で区切って主スレッドを返すと 47〜61 RAF/秒になった。描画は生成完了フラグが立つまで止める（途中の材質を触ると emissiveIntensity で 49 件のエラーが出た）。
- タイトル画面は遠景しか映さないのに、影・雲・草木4万本をそのまま描いていた。ヘッドレスの実測で 3〜4 fps。影と雲と草木をタイトル中だけ止める。
- 道が短いのに山頂の高さを後ろ向きに埋めると、登山口が 1,571m から 2,345m へ持ち上がり、スタートが樺平小屋の前になる。小屋カメラもそこで起動する。折り返しで距離を稼ぎ、登山口の高さは上げない。実測はスタート 1,571m、樺平小屋まで 4.8km、山頂 3,026m。

## 2026-09-26

- **沢と丸太橋が「生の地形 `heightAt()`」基準だったのに、プレイヤーが歩く登山道は別の高さで浮いていた。** 実測（沢の地点）: 地形 338.98 / 道の面 339.52 / 水面 339.40 / 丸太の中心 339.60。道は平滑化した `pts[i].h + 0.12` で、この地点で地形より 0.54〜1.69m 高い。結果、丸太（半径0.22）は道に半分埋まって見えず、水面は下流側で 340.00 まで上がって道の 339.83 を突き抜け、道が途切れて見えた。**地形に置く飾りと、道に置く飾りは基準の高さが違う。道の上に置くものは `walkY()` を使う。**
- 沢は幅 2.2m だと道幅 2.5m に完全に隠れる。4.6m にして初めて道の前後に水が見え、「沢を渡っている」と読める。
- **行動制限を全廃止**（斜度リミット `gLim` 0.48/0.35、段差リミット 0.45m、木や岩の当たり判定 `hitSolid`、道を外れたときの速度 ×0.72）。残したのは地図の外へ出ないための無言クランプのみ。実測: 傾き 3.53（約74度）の斜面で 3.5m 登れる（旧仕様では 0.48 超で完全に壁）。`hitSolid` は呼び出し 0 件になった。
- ヘッドレス検証の落とし穴2件。①`dt=Math.min(.05, …)` があるため、swiftshader の低FPSだと「6秒キーを押して 0.5m しか進まない」。移動量で合否を判定してはいけない。登れたか（climbed > 0）で見る。②`begin()` が `setTimeout(700ms)` の中で `S` を作り直すので、開始直後に座標を書き込んでも上書きされる。テレポートは開始 4秒後に、2回に分けて撃つ。
- **登山口→山頂の全区間を機械検査したら、道の36%が山に埋まっていた**（実測 1,946点 / 3,892m / 最悪26.9m）。沢の件とは別の、既存の欠陥。原因は3つ。①折り返しの隣レッグが18〜27mしか離れておらず、地形グリッド **CELL=20m** の1セルに高低差36mの道が2本入る（1,540点）②山小屋の平坦化パッド（半径32m・フラット）が20m隣を通る別レッグを上書きする（257点）③その他（149点）。
- **20mグリッドは、20m以内に並ぶ高低差の大きい2本の道を表現できない。** 節点を下げれば片方が浮き、上げれば片方が埋まる。実験で確認: 道の周囲4節点を道の高さ以下にクランプすると埋没は0になるが、**全5,753点が浮き、最悪38.5m**になった（折り返しの頂点で2本の道が同じ節点を奪い合うため）。地形側で解決しようとしてはいけない。
- **解は「道を設計高に固定しない」こと。** リボンの高さを `pts[i].h + 0.12` 固定ではなく、**できあがった地形の面** `[地面+12cm, 地面+45cm]` の帯に収める。設計高を下限にすると浮き（最悪19.8m）、そのままだと埋没（最悪26.9m）。地形に沿わせると **埋没0・浮き0**。崖の横断勾配で持ち上がらないよう、左右端が押し上げてよい量は45cmまでに制限する。
- 折り返しの間隔は、必要な標高差 = 目標の水平間隔 × その場の地形の傾き、で決める（`MIN_SEP=100`）。埋没は 3,892m → 1,104m に減り、**道の全長は 10,126m → 11,632m（+15%）しか増えない**。登る標高が同じなら、勾配を保つかぎり道の総延長はほぼ変わらないため。
- Playwrightの `waitForFunction(fn, {timeout})` は**第2引数が `arg` なのでタイムアウトが効かない**（30秒で落ちる）。`waitForFunction(fn, null, {timeout})` と書く。地形生成に60秒以上かかるこのゲームでは必須。
